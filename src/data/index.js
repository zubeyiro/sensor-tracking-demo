global.data = {
    users: require('./users.table'),
    sensors: require('./sensors.table'),
    workouts: require('./workouts.table'),
    participants: require('./participants.table'),
    allocations: require('./allocations.table')
}